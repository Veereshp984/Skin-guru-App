# ─── Frontend (Vite) ─────────────────────────────────────────
# Base URL of the FastAPI backend.
# Leave empty to auto-detect (localhost:8000 when frontend is on 5173).
VITE_API_BASE_URL=

# Google OAuth Client ID (same as backend GOOGLE_CLIENT_ID)
VITE_GOOGLE_CLIENT_ID=
